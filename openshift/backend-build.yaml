apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: backend-build
spec:
  runPolicy: Serial
  source:
    type: Git
    git:
      uri: https://github.com/Uliwazeer/Project3-3tier-App.git
    contextDir: backend
  strategy:
    type: Docker
    dockerStrategy:
      dockerfilePath: Dockerfile
  output:
    to:
      kind: ImageStreamTag
      name: backend:latest
  triggers:
    - type: ConfigChange
    - type: ImageChange

